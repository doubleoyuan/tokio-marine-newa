> 僅供內部使用，非經許可不得外流。

# UI 設計檢查清單（依登入頁／險種選擇頁製作經驗整理）

用於後續各階段畫面（車輛資料／被保險人資料／要保人資料／KYC及其他／審查與下載）開發前後的自我檢查，避免重複踩到已發生過的坑。

## 1. PRD 逐列核對，不憑印象套用習慣做法

- 每個欄位的「格式」欄（radio／select／Dropdown／checkbox／date picker…）要跟 PRD 表格逐列核對，不能看功能描述就自己選慣用元件。
  - 例：T牌種類原本錯用 select，PRD 規定是 radio。
- 每個欄位要先確認「歸屬哪一階段」，OCR／查詢工具回傳的資料常跨階段拆分，不能因為某工具在某階段觸發，就把所有回傳欄位都塞進同一個畫面。
  - 例：行照OCR的部分欄位歸階段2（車輛資料）、駕照OCR的4個欄位（姓名/性別/生日/住址）歸階段3（被保險人資料），階段1畫面只留牌照號碼／駕照號碼。

## 2. PRD「情境／結果」表要連異常情境一起做，不只做 happy path

- 逾時、失敗、驗證錯誤訊息、邊界值（當日／早於系統日／超過天數限制）這類情境，跟正常成功路徑同樣要實作，不能只做「上傳成功」就結案。
- 遇到已知的PRD阻礙項（PlanCode清冊Excel缺檔、生效日60天vs3個月規則衝突、高風險職業代碼時效性、上傳白名單/大小上限PRD自己標TODO未定案、廠牌/車系/車款選項資料源未附）時要**主動提醒**使用者一起決定，不要默默處理或略過，詳細清單見 `project_tokio_marine_ui_design.md`。
- PRD 內部本身也可能不一致（不只是PRD↔SD），例如附錄BDD案例跟前面章節的功能描述/欄位表互相矛盾，遇到時要指出矛盾點、給合理判斷依據，讓使用者確認後再繼續，不要照單全收。

## 3. 視覺一致性要肉眼比對，不能只驗證功能

- 新增表單元件時（尤其是 select／input），要跟同一畫面上其他已知套好樣式的欄位並排比對，確認有沒有漏接共用 class。
  - 例：`.plan-block select` 只設了 `flex:1`，忘了接 padding／border-radius／border，畫面上像完全沒套 CSS 的原生下拉選單。
- 每次改 `components.css` 或畫面樣式，要同步檢查 Design System 頁面（`robins-design-system.html`）的對應元件展示是否還跟畫面一致，不要讓兩邊分岔（元件寫法改了，展示區的 demo markup 卻沒跟著換）。
- 如果某個共用欄位的邏輯（例如下拉選單的某個選項要阻擋）是在做新頁面時才發現舊頁面沒做對，要**回頭把舊頁面一起修**，不要只修新頁面，造成同一個欄位在不同階段行為不一致。

## 4. 畫面內容規範

- 畫面只放使用者在正式產品中真的會看到的文案；「這是demo」「這裡還沒定案」「這是原型，點了會怎樣」這類說明文字一律不進畫面 HTML，只寫進對話 / memory。
- 欄位對照 API 這類工程資訊只放 Design System 的 `#fieldmap` 章節，不要出現在畫面上（畫面上不放 mono 小字備註）。
- 需要多狀態demo互動時（如OCR結果循環、廠代碼查詢忙碌/失敗切換），用「按鈕可重複點擊示範不同結果 + `title` tooltip 提示」的既有手法，不要另外發明新的展示方式。
- **判斷準則**：只有依附在「真實產品中使用者本來就會重複操作」的按鈕上（上傳OCR、查詢廠代碼）才能做成可循環demo；「送出／送出投保」這種真實情境下只會做一次的動作，只能固定示範happy path，其餘結果（錯誤碼、審查結果等）移到 Design System 的元件展示區集中收錄，不要放在真實畫面的送出按鈕上循環（階段2、階段6都曾經因為忘記這條而要回頭修正）。

## 5. 既有的技術細節慣例

- 標題「N. 名稱」的數字與句點要包一層 `<span style="font-family:'Roboto',sans-serif;">`，避免 Noto Sans TC 把半形符號畫成全形。
- 底部固定操作列要用 `body{display:flex;flex-direction:column;min-height:100vh}` + `.shell{flex:1 0 auto}`，避免內容不足時視窗底部露出空白。
- OCR／證件上傳統一沿用：滿版圖 + 下方文字 + 可點擊放大 lightbox；「待辨識」用 primary 藍色系、「辨識完成」用 secondary 青綠色系並在檔名前加實心打勾 icon，不要退回同色系。
- 本機驗證時優先用 DOM／computed style 檢查（`getBoundingClientRect`／`getComputedStyle`），不要只依賴 screenshot（背景分頁截圖偶爾會回傳空白／舊畫面，屬工具限制不是真的畫面問題）。

## 6. 交付流程

1. 本機起 `python3 -m http.server` 從專案根目錄serve，在 Browser pane 開啟並互動測試（點擊走過所有狀態）。
2. 確認無誤後同步更新 Design System 頁面的對應元件展示。
3. 兩邊（畫面 Artifact + Design System Artifact）都重新發布，畫面 Artifact 記得帶最新的 `components.css` / `design-tokens.css` / assets。
4. 更新 `project_tokio_marine_ui_design.md` memory log，記錄本次決策與改動範圍。

## 7. 延後決定、之後要記得回頭問使用者的事項

- **角色權限管理畫面**：專案後期（畫面/功能大致底定後）要再跟使用者確認一次「先不做、後台直接維護」的決定沒有改變。
- **案件總覽的「編輯」畫面**：6個階段頁面都做完後，要回來跟使用者討論編輯頁要複用六階段流程還是另外做唯讀/編輯合一頁面。
- **險種選擇頁遺留項目**：
  - 保險生效日欄位目前是純文字 input，PRD 格式規定是 date picker，且缺「生效日→到期日自動算+1年並轉唯讀」的連動、「當日不可選/早於系統日鎖定/距今超過60天」三種驗證訊息與日期封鎖。
  - 目前「下一步」按鈕只是導頁，沒有「送出預查」的動作概念，也沒有「預查逾時，請稍後再試，已填資料保留」的錯誤狀態展示。

## 8. 已知架構限制（給接手開發人員參考）

- 六個階段畫面都是**獨立的靜態HTML**，彼此沒有真正的跨頁資料傳遞（不是SPA、也沒有用localStorage/sessionStorage存值）——每頁能「看起來」資料有連貫，是因為所有頁面都用同一組寫死的demo資料（例如王小明/A123456789/AAA-0001），不是真的讀取使用者在前面頁面實際輸入的內容。
- 這在階段6「資料總覽（複核）」頁最明顯：那頁的總覽內容是寫死的，不會反映使用者在階段1~5實際修改過的欄位。這是刻意的決定（這個階段的目標是讓PO/開發人員確認流程與欄位，不是做出能真正運作的系統），開發人員接手時要注意：**畫面上的「資料總覽」邏輯需要重新串接真實的表單狀態，不能直接照抄這裡的寫死內容**。
